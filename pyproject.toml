[project]
name = "dspace-reporting"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "psycopg2-binary",
  "pandas",
  "matplotlib",
  "pyyaml",
  "urllib3",
  "sshtunnel==0.4.0",
  "paramiko==2.11.0",
  "notebook",
]

[tool.setuptools.packages.find]
where = ["src"]
